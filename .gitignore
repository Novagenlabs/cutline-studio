node_modules
dist

# Next.js build output
.next
next-env.d.ts

# Built by `npm run build:cutter` from src/main.ts
public/cutline/app.js

# Secrets — never commit. Neon URLs and Stripe keys live here.
.env
.env.*
!.env.example
